<Page x:Class="BatchCutting_DG.D_Presentation.Views.Pages.Page10"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BatchCutting_DG.D_Presentation.Views.Pages"
      mc:Ignorable="d" 
      Title="Page10">

    <Grid x:Name="PageGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="43"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Première ligne -->
        <TextBlock x:Name="ListTitle" Grid.Row="0" Grid.Column="0" Text="{DynamicResource P10_00}"/>

        <!-- Deuxième ligne -->
        <Border x:Name="ListTitleBorder" Grid.Row="1" Grid.Column="0">
            <TextBlock x:Name="ListTitleHeader" Text="{DynamicResource P10_01}"/>
        </Border>

        <!-- Troisième ligne -->
        <!-- Colonne 1 -->
        <ScrollViewer x:Name="ListScrollViewer" Grid.Row="2" Grid.Column="0">
            <ListView x:Name="ListView" ItemsSource="{Binding LotsADecouper}" SelectedItem="{Binding SelectedLot, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Designation}" Width="300"/>
                            <TextBlock Text="{Binding NombreDecoupe}" Width="100"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>

    </Grid>
</Page>
