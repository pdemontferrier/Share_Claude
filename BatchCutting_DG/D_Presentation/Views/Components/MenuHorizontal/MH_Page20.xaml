<UserControl x:Class="BatchCutting_DG.D_Presentation.Views.Components.MenuHorizontal.MH_Page20"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BatchCutting_DG.D_Presentation.ViewModels.Components.MenuHorizontal"
             xmlns:local="clr-namespace:BatchCutting_DG.D_Presentation.Utilities"
>
    <UserControl.Resources>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </UserControl.Resources>
    
    <Grid x:Name="MH_Grid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="MH_Grid_C1"/>
            <ColumnDefinition x:Name="MH_Grid_C2"/>
        </Grid.ColumnDefinitions>

        <Border x:Name="MH_Border" Grid.Column="1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Button x:Name="MH_Home" Command="{Binding HomePage}" Background="Transparent" BorderBrush="Transparent" Visibility="Collapsed">
                        <StackPanel Orientation="Vertical">
                            <Image x:Name="MH_Home_Icon"/>
                            <TextBlock  x:Name="MH_Home_Text" Text="{DynamicResource MH_Ti_17}"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="MH_Previous"  Command="{Binding PreviousCommand}" Background="Transparent" BorderBrush="Transparent">
                        <StackPanel Orientation="Vertical">
                            <Image x:Name="MH_Previous_Icon"/>
                            <TextBlock  x:Name="MH_Previous_Text" Text="{DynamicResource MH_Ti_02}"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="MH_Refresh" Command="{Binding RefreshCommand}" Background="Transparent" BorderBrush="Transparent">
                        <StackPanel Orientation="Vertical">
                            <Image x:Name="MH_Refresh_Icon"/>
                            <TextBlock  x:Name="MH_Refresh_Text" Text="{DynamicResource MH_Ti_03}"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
                <StackPanel x:Name="MH_StackPanel" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="MH_Details" Command="{Binding DetailBatch}" Background="Transparent" BorderBrush="Transparent" Visibility="Collapsed">
                        <StackPanel Orientation="Vertical">
                            <Image x:Name="MH_Details_Icon"/>
                            <TextBlock  x:Name="MH_Details_Text" Text="{DynamicResource MH_Ti_12}"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="MH_Refuse" Command="{Binding RefusePage}" Background="Transparent" BorderBrush="Transparent" Visibility="Collapsed">
                        <StackPanel Orientation="Vertical">
                            <Image x:Name="MH_Refuse_Icon"/>
                            <TextBlock  x:Name="MH_Refuse_Text" Text="{DynamicResource MH_Ti_05}"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="MH_Validate" Command="{Binding ValidateBar}" Background="Transparent" BorderBrush="Transparent" Visibility="Collapsed"
                            IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBooleanConverter}}">
                        <StackPanel Orientation="Vertical">
                            <Image x:Name="MH_Validate_Icon"/>
                            <TextBlock  x:Name="MH_Validate_Text" Text="{DynamicResource MH_Ti_06}"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="MH_Menu" Command="{Binding MenuCommand}" Background="Transparent" BorderBrush="Transparent">
                        <StackPanel Orientation="Vertical">
                            <Image x:Name="MH_Menu_Icon"/>
                            <TextBlock  x:Name="MH_Menu_Text" Text="{DynamicResource MH_Ti_01}"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
